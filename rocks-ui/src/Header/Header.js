import React from 'react'

const Header = (props) => (
  <div className="jumbotron">
    <h1 class="display-4">Rocks</h1>
    <p class="lead">A list of rock apperances, locations and other properties</p>
  </div>
);

export default Header;