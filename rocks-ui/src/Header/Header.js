import React from 'react'

const Header = (props) => (
  <div className="jumbotron">
    <h1 className="display-4">Rocks</h1>
    <p className="lead">A list of rock apperances, locations and other properties</p>
  </div>
);

export default Header;